#!/bin/bash

# Shared command list for help text and completions.
MACKIT_COMMANDS=(
    "flush:Flush junk — free up disk space"
    "wipe:Wipe apps — remove completely"
    "turbo:Turbo boost — check and maintain system"
    "scan:Scan disk — explore usage"
    "matrix:Matrix — live system health monitor"
    "nuke:Nuke — remove project build artifacts"
    "trace:Trace — find and remove installer files"
    "auth:Auth — configure Touch ID for sudo"
    "shell:Shell — setup tab completion"
    "sync:Sync — update to latest version"
    "eject:Eject — remove MacKit from system"
    "help:Show help"
    "version:Show version"
)
